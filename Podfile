# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FinalExam' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FinalExam

  target 'FinalExamTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FinalExamUITests' do
    inherit! :search_paths
    # Pods for testing
  end

    pod 'Alamofire'
    pod 'SwiftyJSON'
    pod 'PINRemoteImage'
    pod 'Player'
end
